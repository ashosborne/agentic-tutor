# Set to true to use fixture LLM responses (no API keys needed)
DEMO_MODE=true

# Required when DEMO_MODE=false (worksheet images + scan assessment)
OPENAI_API_KEY=

# Optional / unused by current agents
ANTHROPIC_API_KEY=

# Server
PORT=5173
HOST=0.0.0.0
